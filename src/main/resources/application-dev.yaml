# ─────────────────────────────────────────────
# application-dev.yml  —  local / development
# ─────────────────────────────────────────────

spring:
  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java
      exclude: static/**,public/**
    livereload:
      enabled: true

server:
  port: 8081                          # Different from prod default
  error:
    include-message: always
    include-stacktrace: always
    include-binding-errors: always
  tomcat:
    accesslog:
      enabled: true
      pattern: '%h %l %u %t "%r" %s %b'

management:
  endpoints:
    web:
      exposure:
        include: "*"                  # Dev only - all endpoints
  endpoint:
    health:
      show-details: always
    loggers:
      enabled: true
    metrics:
      enabled: true

logging:
  level:
    root: INFO
    com.apiCodeGenerator: DEBUG
    org.springframework.web: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"

# Generator REST endpoints are ONLY available in dev profile.
generator:
  # Where generated Java files are written.
  # Relative to project root (where you run the app from).
  output-root: src/main/java

  # Directory containing module JSON config files.
  # Files must be named {module_name}.json
  config-dir: src/main/resources/generator-configs

# JWT - Dev key (different from prod)
app:
  jwt:
    secret: ${DEV_JWT_SECRET:Q3Y1bW9wN213eHpxMnRmdTZhYjM0cnk4OTAwM25iMGQxcGxlNmF1cjRrZXk4NzY1NDMyMTEwYmFzZTY0c2VjcmV0MjAyNQ==}
    expiration-time: ${DEV_JWT_EXPIRATION:604800000}

# Mail Configuration
mail:
  host: email-smtp.eu-west-2.amazonaws.com
  port: 587
  username: AKIA6QB7T3KA3JZ42VJO
  password: BP0V2bQAKikSVZ1A3oiBgec9Ybd/jqEN3nD0lka9cFQW
  from: donotreply@novumgen.uk
  fromname: RI(Regulatory-Intelligence)
  smtp:
    auth: true
    starttls:
      enable: true
    ssl:
      trust: email-smtp.eu-west-2.amazonaws.com

# File Upload Configuration
file:
  upload:
    max-size: 52428800
    local-dir: uploads/
    allowed-types: "*"    # image/jpeg, image/png, application/pdf

# ⚠️ REMOVED hardcoded mail, spaces, audit configs
# These should NEVER be in YAML files
# Use Spring Cloud Config + Vault instead